[package]
name = "mock_jupiter_router"
version = "0.1.0"
description = "Test-only Jupiter-compatible CPI router for StockSplit integration tests"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.3.0"
